<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <receiver
            android:name=".CallReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.absapp.action.ACCEPT_CALL" />
                <action android:name="com.absapp.action.DECLINE_CALL" />
            </intent-filter>
        </receiver>
    </application>
</manifest>